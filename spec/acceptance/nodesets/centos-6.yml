HOSTS:
  centos-6.example.com:
    roles:
      - master
    platform: el-6-x86_64
    image : centos:centos6
    hypervisor : docker
    docker_image_commands:
      - 'yum -y install which tar ntpdate'
CONFIG:
  log_level: debug
  type: git
